
// classe para definir as entidades de todo o sistema. Neste caso este é o AIresponse, 
//ou seja, a resposta da AI, que é uma string
class AiResponse {
  final String textResponse;

  const AiResponse(this.textResponse);
}